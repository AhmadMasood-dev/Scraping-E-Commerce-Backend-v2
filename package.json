{
  "name": "pqc-backend-v2",
  "version": "0.1.0",
  "private": true,
  "description": "PQC v2 — search-driven price & quality comparison backend",
  "main": "src/server.js",
  "scripts": {
    "test": "node --test",
    "start": "node --env-file=.env src/server.js",
    "dev": "node --env-file=.env --watch src/server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1"
  }
}
