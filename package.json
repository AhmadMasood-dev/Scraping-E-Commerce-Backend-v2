{
  "name": "pqc-backend-v2",
  "version": "0.1.0",
  "private": true,
  "description": "PQC v2 — search-driven price & quality comparison backend",
  "main": "src/server.js",
  "scripts": {
    "test": "node --test",
    "dev": "node --watch src/server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
